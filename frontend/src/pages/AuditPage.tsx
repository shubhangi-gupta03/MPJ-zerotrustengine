import { PageShell } from "../components/PageShell";
export default function AuditPage() { return <PageShell title="Audit Logs"><div className="glass-card p-4">Filterable audit table and JSON event modal.</div></PageShell>; }
